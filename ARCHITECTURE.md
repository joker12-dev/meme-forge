# 📊 Architecture & Flow Diagrams

## 🏗️ System Architecture

```
┌───────────────────────────────────────────────────────────────────────────┐
│                    MEME TOKEN PLATFORM - BSC TESTNET                      │
└───────────────────────────────────────────────────────────────────────────┘

                              EXTERNAL USERS
                                   ▲
                                   │
                    ┌──────────────┴──────────────┐
                    │                             │
            ┌───────▼──────┐             ┌───────▼──────┐
            │  FRONTEND    │             │  EXTERNAL IP │
            │  React App   │◄────────────┤  78.184.163  │
            │  Port 3000   │             │     .223     │
            └───────┬──────┘             └──────────────┘
                    │
                    │ HTTP/JSON
                    │
            ┌───────▼──────────────┐
            │  BACKEND API         │
            │  Express.js          │
            │  Port 3001           │
            │                      │
            │ ✅ Rate Limiting     │
            │ ✅ Validation        │
            │ ✅ CORS              │
            │ ✅ Error Handling    │
            └───────┬──────────────┘
                    │
        ┌───────────┼───────────┐
        │           │           │
    ┌───▼───┐  ┌───▼───┐  ┌───▼──────┐
    │  DB   │  │ RPC   │  │ EventLog │
    │ PG SQL│  │ Node  │  │ Parser   │
    └───────┘  └───┬───┘  └──────────┘
                    │
            ┌───────▼─────────────────┐
            │  BSC TESTNET            │
            │  ChainID: 97            │
            │  RPC: data-seed-prebsc  │
            └───────┬─────────────────┘
                    │
        ┌───────────┼───────────┐
        │           │           │
    ┌───▼───┐  ┌───▼────┐  ┌───▼──────┐
    │Contract │  │Factory │  │Liquidity │
    │ Models │  │Contract│  │Adder     │
    └────────┘  └────────┘  └──────────┘
```

---

## 🔄 Token Creation Flow (Detailed)

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    TOKEN CREATION PROCESS                               │
└─────────────────────────────────────────────────────────────────────────┘

Step 1: User Fills Form
┌──────────────────────────────┐
│ Frontend Token Creation Form │
│                              │
│ Name: "My Token"             │
│ Symbol: "MYT"                │
│ Supply: 1,000,000            │
│ Description: "..."           │
└────────────┬─────────────────┘
             │
             ▼
┌──────────────────────────────┐
│ Validation                   │
│ ✅ Input sanitization       │
│ ✅ Rate limit check         │
│ ✅ Uniqueness check         │
└────────────┬─────────────────┘
             │
             ▼
┌──────────────────────────────┐
│ API Call                     │
│ POST /api/tokens/create      │
└────────────┬─────────────────┘
             │
             ▼
Step 2: Backend Processing
┌──────────────────────────────┐
│ Backend Handler              │
│ routes/tokens.js             │
│                              │
│ 1. Validate request          │
│ 2. Check user auth           │
│ 3. Store in database         │
└────────────┬─────────────────┘
             │
             ▼
┌──────────────────────────────┐
│ Call Smart Contract          │
│ TokenFactory.createToken()   │
└────────────┬─────────────────┘
             │
             ▼
Step 3: Smart Contract Execution
┌──────────────────────────────┐
│ TokenFactory Contract        │
│ (Polygon/Ethereum address)   │
│                              │
│ 1. Validate parameters       │
│ 2. Deploy MemeToken clone    │
│ 3. Call constructor:         │
│    - owner = creator         │
│    - initialSupply = 1M       │
│    - platformWallet = 0x41...│
└────────────┬─────────────────┘
             │
             ▼
┌──────────────────────────────┐
│ MemeToken Deployment         │
│                              │
│ NEW: Initialize              │
│ address mintRecipient =      │
│   (platformWallet != 0)      │
│   ? platformWallet           │
│   : owner                    │
│                              │
│ _mint(mintRecipient,         │
│       initialSupply)         │
│                              │
│ ✅ RESULT:                   │
│ 1M tokens → PLATFORM WALLET  │
│ NOT to creator!              │
└────────────┬─────────────────┘
             │
             ▼
Step 4: Backend Stores Info
┌──────────────────────────────┐
│ Database Insert              │
│ Token model {                │
│   address: 0x...,            │
│   creator: 0x...,            │
│   name: "My Token",          │
│   symbol: "MYT",             │
│   supply: 1000000,           │
│   status: "created",         │
│   lpAdded: false,            │
│   ...                        │
│ }                            │
└────────────┬─────────────────┘
             │
             ▼
Step 5: Frontend Receives Response
┌──────────────────────────────┐
│ API Response                 │
│ {                            │
│   success: true,             │
│   data: {                    │
│     tokenAddress: 0x...,     │
│     txHash: 0x...,           │
│     creator: 0x...,          │
│     createdAt: timestamp     │
│   }                          │
│ }                            │
└────────────┬─────────────────┘
             │
             ▼
┌──────────────────────────────┐
│ Frontend Shows Result        │
│                              │
│ ✅ Token Created!            │
│ Address: 0x...               │
│ Supply: 1,000,000 MYT        │
│ Status: Ready for liquidity  │
│                              │
│ Next: Add Liquidity          │
│ [Button] Approve & Add LP    │
└──────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ AT THIS POINT:                                                          │
│ ✅ Token deployed                                                       │
│ ✅ 1M tokens in PLATFORM wallet (NOT creator)                           │
│ ✅ Creator is contract owner but has 0 tokens                           │
│ ⏳ Liquidity NOT added yet                                              │
│ ⏳ Trading NOT available yet                                            │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 💧 Add Liquidity Flow (New Process)

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    ADD LIQUIDITY PROCESS                                │
└─────────────────────────────────────────────────────────────────────────┘

Step 1: Platform Approves LiquidityAdder
┌──────────────────────────────┐
│ Platform Wallet              │
│ (Has 1M tokens from creation)│
│                              │
│ Calls: Token.approve(        │
│   spender: LiquidityAdder,   │
│   amount: 999999...          │ ← Large amount or infinite
│ )                            │
└────────────┬─────────────────┘
             │
             ▼ TX on blockchain
┌──────────────────────────────┐
│ MemeToken Contract           │
│ Sets allowance[platform][LA] │
│ = approved amount            │
└────────────┬─────────────────┘
             │
             ▼
┌──────────────────────────────┐
│ ✅ APPROVED STATE            │
│                              │
│ Platform: 1M tokens          │
│ LiquidityAdder allowance: ✅ │
│ Ready for addLiquidityFrom() │
└──────────────────────────────┘

Step 2: Platform Calls addLiquidityFrom()
┌──────────────────────────────┐
│ LiquidityAdder Contract      │
│ .addLiquidityFrom(           │
│   token: 0xToken,            │
│   from: Platform,   ──────┐  │
│   amount: 500k,    ──┐    │  │
│   recipient: Creator │    │  │
│ )                    │    │  │
└────────────┬─────────┼────┼──┘
             │        │    │
             │        │    └─ Creator gets LP tokens
             │        └──────── 500k tokens from platform
             │
             ▼
Step 3: LiquidityAdder Executes

┌──────────────────────────────┐
│ LiquidityAdder.addLiquidityFrom()
│                              │
│ A. Pull Tokens               │
│    token.transferFrom(       │
│      from: Platform,         │
│      to: LiquidityAdder,     │
│      amount: 500k            │
│    )                         │
│    ✅ Uses allowance         │
└────────────┬─────────────────┘
             │
             ▼
┌──────────────────────────────┐
│ B. Calculate Fee             │
│                              │
│ tokenAmount = 500k           │
│ fee = 500k * 1% = 5k         │
│ toLiquidity = 500k - 5k      │
│            = 495k            │
└────────────┬─────────────────┘
             │
             ▼
┌──────────────────────────────┐
│ C. Add Liquidity to          │
│    PancakeSwap               │
│                              │
│ router.addLiquidityETH(      │
│   token: 0xToken,            │
│   amountTokenDesired: 495k,  │
│   amountTokenMin: 490k,      │
│   amountETHMin: 0.9 BNB,     │
│   to: LiquidityAdder,  ◄─────┼─ LP tokens go here
│   deadline: now + 20min      │
│ )                            │
└────────────┬─────────────────┘
             │
             ▼
┌──────────────────────────────┐
│ D. Handle LP Tokens          │
│                              │
│ LPTokens received by         │
│ LiquidityAdder               │
│                              │
│ Send to LPLocker:            │
│ lpLocker.lockLP(             │
│   lpToken: 0xLP,             │
│   amount: lpTokens,          │
│   unlockTime: future,        │
│   owner: Creator             │
│ )                            │
└────────────┬─────────────────┘
             │
             ▼
┌──────────────────────────────┐
│ E. Refund Unused             │
│                              │
│ Unused tokens back to        │
│ Platform wallet              │
│                              │
│ Unused ETH back to           │
│ caller (platform)            │
└────────────┬─────────────────┘
             │
             ▼
Step 4: Result

┌──────────────────────────────────────────────────┐
│ TRANSACTION COMPLETE                             │
│                                                  │
│ Platform Wallet:                                 │
│ • Started: 1,000,000 tokens                      │
│ • After:   500,005,000 tokens (minus 495k)      │
│ • Plus:    refunded unused ETH                   │
│                                                  │
│ Creator:                                         │
│ • LP tokens locked in LPLocker                  │
│ • Can't sell LP tokens (locked)                 │
│ • Receives LP rewards when unlocked              │
│                                                  │
│ PancakeSwap Pool:                                │
│ • 495k tokens + 1 BNB liquidity added           │
│ • LIVE for trading                              │
│ • Slippage calculated normally                   │
│                                                  │
│ Platform Wallet:                                 │
│ • Keeps 5k tokens as fee                         │
│                                                  │
└──────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────┐
│ FINAL STATE:                                     │
│                                                  │
│ Token: TRADEABLE ✅                             │
│ Liquidity: LOCKED ✅ (can't be removed)         │
│ Creator: RECEIVED LP ✅ (can't rug)            │
│ Platform: CONTROLLED FLOW ✅                    │
│                                                  │
│ NO RUG PULL POSSIBLE ✅                         │
└─────────────────────────────────────────────────┘
```

---

## 🏛️ Contract Interaction Diagram

```
┌──────────────────────────────────────────────────────────┐
│           SMART CONTRACT RELATIONSHIPS                   │
└──────────────────────────────────────────────────────────┘

User (Frontend)
    │
    │ creates token via
    ▼
┌─────────────────┐
│ TokenFactory    │  ◄─── Owns template address
│                 │
│ createToken()   │
└────────┬────────┘
         │ deploys clone
         ▼
┌─────────────────────────────────────────────┐
│ MemeToken (Clone 1)                         │
│ 0x123...                                     │
│                                              │
│ Properties:                                  │
│ • owner = Creator                            │
│ • balances[platform] = 1,000,000            │
│ • balances[creator] = 0                      │
│ • totalSupply = 1,000,000                    │
└────────┬─────────────────────────────────────┘
         │ platform approves
         ▼
┌─────────────────────────────────────────────┐
│ LiquidityAdder                               │
│ 0xE647...                                    │
│                                              │
│ Function: addLiquidityFrom(                 │
│   token,                                     │
│   from (platform),                           │
│   amount,                                    │
│   recipient (creator)                        │
│ )                                            │
│                                              │
│ Calls:                                       │
│ • token.transferFrom(platform, this, amount)│
│ • router.addLiquidityETH(...)               │
│ • lpLocker.lockLP(...)                       │
└──────────────────────────────────────────────┘
         │ pulls tokens
         ▼
    MemeToken
    (approvals used)
         │ gets LP tokens
         ▼
┌──────────────────────────────────┐
│ PancakeRouter                    │
│ (External: 0xD99D...)            │
│                                  │
│ Creates liquidity pool           │
│ MemeToken / WBNB                 │
└──────────────────────────────────┘
         │ locks LP tokens
         ▼
┌──────────────────────────────────┐
│ LPLocker                         │
│ (External contract)              │
│                                  │
│ owner = Creator                  │
│ lockedUntil = future timestamp   │
│ canClaim = false (until unlock)  │
└──────────────────────────────────┘
```

---

## 🔐 Security Flow

```
┌──────────────────────────────────────────────────────────┐
│              SECURITY LAYERS                             │
└──────────────────────────────────────────────────────────┘

Frontend Input
    │
    ▼ LAYER 1: Frontend Validation
┌──────────────────────────┐
│ React component          │
│ • Type checking          │
│ • Length validation      │
│ • Format validation      │
└──────────────┬───────────┘
               │
               ▼ LAYER 2: Network
┌──────────────────────────┐
│ HTTP Request             │
│ • CORS check             │
│ • Header validation      │
└──────────────┬───────────┘
               │
               ▼ LAYER 3: Backend Route
┌──────────────────────────┐
│ Express middleware       │
│ • Auth check             │
│ • Rate limit check       │
│ • Request size limit     │
└──────────────┬───────────┘
               │
               ▼ LAYER 4: Input Validator
┌──────────────────────────┐
│ validators.js            │
│ • Sanitize strings       │
│ • Validate numbers       │
│ • Check ranges           │
│ • Prevent injection      │
└──────────────┬───────────┘
               │
               ▼ LAYER 5: Business Logic
┌──────────────────────────┐
│ Controller function      │
│ • User auth check        │
│ • Permission check       │
│ • Duplicate check        │
│ • Database constraints   │
└──────────────┬───────────┘
               │
               ▼ LAYER 6: Smart Contract
┌──────────────────────────┐
│ Contract validation      │
│ • onlyOwner checks       │
│ • Reentrancy guards      │
│ • Transfer checks        │
│ • Amount validations     │
└──────────────────────────┘

RESULT: ✅ Multi-layered protection
```

---

## 📊 Data Flow

```
User Creates Token
    │
    ├─→ Frontend State
    │   • formData = {name, symbol, ...}
    │
    ├─→ API Request
    │   • POST /api/tokens/create
    │   • Body: {name, symbol, ...}
    │
    ├─→ Backend Processing
    │   • Validation
    │   • Database insert
    │   • Smart contract call
    │
    ├─→ Blockchain
    │   • TokenFactory.createToken()
    │   • MemeToken deployed
    │   • Tokens minted to platform
    │
    ├─→ API Response
    │   • {tokenAddress, txHash, ...}
    │
    └─→ Frontend State Update
        • showSuccess = true
        • newToken = {...}
        • redirect to token page
```

---

## 🎯 Summary

- **Frontend**: React SPA at port 3000
- **Backend**: Express API at port 3001
- **Contracts**: Three main contracts on BSC Testnet
- **Flow**: Simple token creation → Platform controls supply → Platform adds liquidity
- **Security**: Multi-layer validation and smart contract protection
- **Result**: No rug pull possible, secure and transparent

**All components are live and integrated! 🚀**
